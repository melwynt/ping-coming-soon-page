{
  "name": "ping-coming-soon-page",
  "version": "1.0.1",
  "description": "Frontend Mentor Challenge - Ping Coming Soon Page",
  "scripts": {
    "start": "rm -rf public/* && parcel src/index.html --dist-dir public --port 1234 --hmr-port 4326 --host localhost --open",
    "build": "rm -rf public/* && parcel build src/index.html --dist-dir public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "prettier": {
    "singleQuote": true
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.2",
    "parcel": "latest"
  },
  "dependencies": {
    "email-validator": "^2.0.4",
    "react": "latest",
    "react-dom": "latest"
  }
}
